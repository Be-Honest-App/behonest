# PageId

Purpose
- Small utility component that renders or exposes a page identifier. Useful for debugging or analytics hooks.

Path
- `app/components/PageId.tsx`

Props (typical)
- `id?: string`

Usage
```tsx
<PageId id="home" />
```

Notes
- Minimal component; keep implementation simple.
