# ShoutLink

Purpose
- Component used to create a short 'shout' or link post — typically shows a form and preview.

Path
- `app/components/ShoutLink.tsx`

Props (typical)
- `onSubmit?: (data: any) => void`

Usage
```tsx
<ShoutLink />
```

Notes
- Keep the preview small and provide validation for URL fields if present.
