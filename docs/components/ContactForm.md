# ContactForm

Purpose
- A simple contact form used on the contact page. Collects name, email and message and submits to a server endpoint or email service.

Path
- `app/components/ContactForm.tsx`

Props (typical)
- none — self-contained form component

Usage
```tsx
<ContactForm />
```

Notes
- Validate email and required fields before submitting. The example in this repo is primarily presentational.
