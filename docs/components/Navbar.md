# Navbar

Purpose
- Top navigation bar including brand, primary navigation links, and theme toggle.

Path
- `app/components/Navbar.tsx`

Props (typical)
- None required; the component is used globally in `layout.tsx`.

Usage
```tsx
<Navbar />
```

Notes
- Integrates `ThemeToggle` to switch between light/dark themes.
