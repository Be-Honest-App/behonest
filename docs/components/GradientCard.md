# GradientCard

Purpose
- Small UI card with a gradient background used in `app/components/ui/GradientCard.tsx`.

Path
- `app/components/ui/GradientCard.tsx`

Props (typical)
- `title: string`
- `subtitle?: string`

Usage
```tsx
<GradientCard title="Metric" subtitle="Details" />
```

Notes
- Keep styles consistent with Tailwind utility classes.
